---
title: Bugs & Limitations
description: Known constraints in Margrete RPC, and how to work around them.
icon: TriangleAlert
---

## Event scanning

Because of a [limitation in the Margrete](https://github.com/inonote/umiguri-issue-repo/issues/6), plugin scans for events instead of reading them directly. This has two practical consequences:

- **Events far past the last note may not be read.** By default, the scan covers up to 19200 ticks beyond the last note.
- **Charts with many events can be slow to open.** [_Dead Soul_](https://pgko.dev/bundles/019cafe9-6901-7000-a8e5-0785150fdf94) is a known example containing many scroll-speed event ([`TimelineSpeedEvent`](/docs/reference/chart/events/TimelineSpeedEvent)).

### Workarounds

Use the `open_edit` parameters described in [Edit Transactions](/docs/basic/edit-transactions#open_edit-options) to control the scan:

```python
with m.open_edit(
    # ticks to scan past the last note (default: 19200)
    event_scan_lookahead_ticks=1920 * 4,
) as tx:
    ...
```

Or provide an explicit list of TIL to scan:

```python
with m.open_edit(event_scan_til_ids=[0, 1, 2]) as tx:
    ...
```

Pass `event_scan_til_ids=[]` to scan common TIL (`0 — 15`).

## Undo note deletion

When you code **deletes** notes, pressing Undo in Margrete may restore those notes in a duplicated state instead of cleanly reversing the deletion.

### Workarounds

- Use [`Margrete.undo()`](/docs/reference/client/Margrete#functions) instead of undoing in Margrete.
