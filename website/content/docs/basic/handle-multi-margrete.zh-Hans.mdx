---
title: 多个 Margrete
description: 处理多个正在运行的插件实例。
---

当只发现一个正在运行的插件实例时，不带参数调用 [`Margrete()`](/docs/reference/client/Margrete) 就能正常工作。

如果同时运行了多个 Margrete 实例，请传入 `endpoint` 或 `instance_id`。

如果需要先查看可用的 [`MargreteInstance`](/docs/reference/discovery/MargreteInstance)，可以使用 [`list_instances()`](/docs/reference/discovery)：

```python
from margrete_rpc import Margrete, list_instances

# 列出所有发现的实例
for inst in list_instances():
    print(inst.instance_id, inst.endpoint)
    for transport in inst.transports:
        print(" ", transport.type, transport.endpoint)

# 通过显式 TCP endpoint 连接（左侧 Margrete）
with Margrete(endpoint="127.0.0.1:1328") as m_left:
    print(m_left.status())

# 通过 instance_id 连接（右侧 Margrete）
with Margrete(instance_id="47824-6a30f65d-b7f8afde84506008") as m_right:
    print(m_right.status())

# 通过显式 named-pipe endpoint 连接
with Margrete(endpoint="npipe://./pipe/margrete-rpc-47824-6a30f65d-b7f8afde84506008") as m_pipe:
    print(m_pipe.status())
```

显式 endpoint 支持以下格式：

| 格式                              | Transport          |
| --------------------------------- | ------------------ |
| `127.0.0.1:1328`                  | TCP                |
| `tcp://127.0.0.1:1328`            | TCP                |
| `npipe://./pipe/margrete-rpc-...` | Windows named pipe |

![Two Margrete instances with different endpoints](../assets/2-margrete-instance.png)
