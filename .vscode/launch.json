{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/sdk",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/sdk/.venv/Scripts/python.exe"
        }
    ]
}
